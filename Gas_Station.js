

const gas = [1,2,3,4,5]
const cost = [3,4,5,1,2]

var canCompleteCircuit = function(gas, cost) {

    for(let i; i< gas.length ; i++) {
     if(gas[i]-cost[i]<0) {
        return -1
     }
      recursive
    }
};

